<Page x:Class="MovieNet.Views.MovieCommentList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
      xmlns:syncfusion="http://schemas.syncfusion.com/wpf" 
      xmlns:local="clr-namespace:MovieNet.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800" Background="White"
      Title="MovieCommentList">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding GetMovieCommentsCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid>
        <DataGrid Name="MovieCommentsGrid" HorizontalAlignment="Left" VerticalAlignment="Top" Width="800" Height="390" Margin="0,60,0,0" ItemsSource="{Binding Comments}">
            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridColumnHeader}" >
                    <Setter Property="Background" Value="#0088dc"/>
                    <Setter Property="BorderThickness" Value="0,0,1,2"/>
                    <Setter Property="BorderBrush" Value="Black"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="author" Binding="{Binding Author}" Width="267" />
                <DataGridTextColumn Header="movieTitle" Binding="{Binding MovieTitle}" Width="267"/>
                <DataGridTextColumn Header="content" Binding="{Binding content}" Width="267"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>
